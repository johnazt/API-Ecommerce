const userRoutes = require("./users.routes")
const authRoutes = require("./auth.routes")
const productRoutes = require("./products.routes")

module.exports = {
    userRoutes, authRoutes, productRoutes
}